# tym

Como vim ya es mainstream, mejor usar tym. Tambien tiene un file manager similar a netrw, el cual
al igual que en vim con ":Ex" se puede entrar.

## Build

```
g++ -std=c++17 *.cpp -o tym -lncurses
```
